# Liga a camada de mocks (MSW + Socket.IO simulado) e o painel de cenários.
# Padrão: ligado (qualquer valor diferente de "false", incluindo a env var ausente).
# Só desligue se algum dia existir uma API real para apontar.
VITE_ENABLE_MOCKS=true
